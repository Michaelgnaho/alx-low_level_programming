#include <stdio.h>

/**
 * main - this fuction prints from zero to nine
 *
 * Return: 0 when it is a success
 */

int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
		printf("%d", m);
	printf("\n");

	return (0);
}
